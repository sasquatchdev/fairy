/// module that contains code which
/// deals with error handling.
pub mod errs;

/// module that contains logging 
/// (initialization) code as well
/// as some error and warning messages.
pub mod logs;
